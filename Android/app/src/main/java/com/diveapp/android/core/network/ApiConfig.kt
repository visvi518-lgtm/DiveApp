package com.diveapp.android.core.network

/**
 * Points at a local backend during development. Replace with the deployed
 * Render URL via a build variant before shipping.
 */
object ApiConfig {
    const val BASE_URL = "http://10.0.2.2:8000/"
}
