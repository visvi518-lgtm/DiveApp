<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-build-only: allows plain HTTP to local dev backends. Android 9+
     blocks cleartext traffic by default, which otherwise silently breaks
     every request against a local (non-HTTPS) backend during development.
     Never applies to release builds — see src/debug/AndroidManifest.xml. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
